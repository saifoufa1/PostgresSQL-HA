# PostgreSQL HA Monitor Requirements

# Core dependencies
psycopg2-binary==2.9.7
PyYAML==6.0.1
requests==2.31.0
asyncio-magic==0.1.0

# Optional dependencies for enhanced functionality
# Install these for additional features:
# prometheus-client==0.19.0  # For Prometheus metrics export
# schedule==1.2.1            # For scheduled monitoring tasks
# python-dotenv==1.0.0       # For environment variable management

# Development dependencies (for testing and development)
# pytest==7.4.3              # For unit testing
# pytest-asyncio==0.21.1     # For async testing
# black==23.11.0             # Code formatting
# flake8==6.1.0              # Linting
# mypy==1.7.1                # Type checking

# Optional: For email alerts with authentication
# secure-smtplib==0.1.1      # For secure SMTP connections

# Optional: For webhook integrations
# python-json-logger==2.0.7  # For structured JSON logging